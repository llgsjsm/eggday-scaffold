# Shared

Reusable corpora across targets: source checkouts, advisory sets, symbols,
SDKs, and debug builds.

Large checkouts belong here rather than inside a target folder, and should
generally stay out of version control.
